<div class="min-h-screen bg-slate-50">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 py-12">
    <%= link_to "← Back", user_flow_path, class: "text-slate-500 hover:text-slate-700 text-sm" %>

    <div class="mt-8 grid grid-cols-1 lg:grid-cols-12 gap-10 items-start">
      <!-- LEFT: form -->
      <div class="lg:col-span-7">
        <div class="bg-white border border-slate-200 rounded-3xl p-8 sm:p-10 shadow-sm">
          <p class="text-sm font-semibold text-slate-700">ComeInBooked for customers</p>
          <h1 class="mt-2 text-3xl font-semibold tracking-tight text-slate-900">Log in</h1>
          <p class="mt-2 text-slate-600">
            Book beauty, wellness, creatives and local services near you.
          </p>

          <% if flash.alert.present? %>
            <div class="mt-6 rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">
              <%= flash.alert %>
            </div>
          <% end %>

          <%= form_with url: user_session_path, scope: :user, method: :post, local: true, class: "mt-8 space-y-5" do |f| %>
            <%= hidden_field_tag :authenticity_token, form_authenticity_token %>

            <div>
              <label class="block text-sm font-medium text-slate-700 mb-1">Email</label>
              <%= f.email_field :email,
                    autofocus: true,
                    autocomplete: "email",
                    placeholder: "you@example.com",
                    class: "w-full rounded-xl border border-slate-200 bg-white px-4 py-3 focus:outline-none focus:ring-2",
                    style: "--tw-ring-color:#2f66ff;" %>
            </div>

            <div>
              <label class="block text-sm font-medium text-slate-700 mb-1">Password</label>
              <%= f.password_field :password,
                    autocomplete: "current-password",
                    placeholder: "••••••••",
                    class: "w-full rounded-xl border border-slate-200 bg-white px-4 py-3 focus:outline-none focus:ring-2",
                    style: "--tw-ring-color:#2f66ff;" %>
            </div>

            <div class="flex items-center justify-between text-sm">
              <label class="inline-flex items-center gap-2 text-slate-600">
                <%= f.check_box :remember_me, class: "rounded border-slate-300" %>
                Remember me
              </label>

              <%= link_to "Forgot password?",
                  new_user_password_path,
                  class: "text-blue-700 hover:underline font-medium" %>
            </div>

            <button type="submit"
                    class="w-full rounded-xl px-5 py-3 font-semibold text-white shadow-sm"
                    style="background:#2f66ff;">
              Log in
            </button>
          <% end %>

          <div class="mt-6 text-sm text-slate-600">
            New here?
            <%= link_to "Create an account",
                new_user_registration_path,
                class: "text-blue-700 hover:underline font-medium" %>
          </div>
        </div>
      </div>

      <!-- RIGHT: panel -->
      <div class="lg:col-span-5">
        <div class="rounded-3xl p-8 border border-slate-200 bg-white shadow-sm">
          <h2 class="text-xl font-semibold text-slate-900">Why customers love it</h2>
          <p class="mt-2 text-slate-600 text-sm">
            Clean booking pages, fast checkout, and your appointments in one place.
          </p>

          <div class="mt-6 space-y-3 text-sm text-slate-700">
            <div class="rounded-2xl bg-slate-50 border border-slate-200 p-4">⭐ Ratings & reviews (coming soon)</div>
            <div class="rounded-2xl bg-slate-50 border border-slate-200 p-4">📍 Nearby discovery (coming soon)</div>
            <div class="rounded-2xl bg-slate-50 border border-slate-200 p-4">🔔 Reminders (later)</div>
          </div>

          <div class="mt-8 rounded-2xl p-5 text-white"
               style="background: linear-gradient(135deg, #0b1f46 0%, #0f2d60 60%, #2f66ff 140%);">
            <p class="text-sm font-semibold">Tip</p>
            <p class="mt-2 text-white/85 text-sm">
              If you’re a business owner, use the professional login instead.
            </p>
            <div class="mt-4">
              <%= link_to "Go to professional login",
                  partners_sign_in_path,
                  class: "inline-flex items-center justify-center rounded-full px-5 py-2.5 font-semibold bg-white text-slate-900 hover:bg-white/90" %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
