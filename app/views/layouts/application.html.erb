<!DOCTYPE html>
<html>
  <head>
    <title>ComeInBooked</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Premium CSS layer (works WITH Tailwind) -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Tailwind -->
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <!-- Mapbox token (safe even if unused) -->
    <script>
      mapboxgl && (mapboxgl.accessToken = "<%= ENV['MAPBOX_ACCESS_TOKEN'] %>");
    </script>
  </head>

  <body class="bg-white text-slate-900 antialiased">
    <%= render "shared/navbar" %>
    <%= render "shared/flash" %>

    <main>
      <%= yield %>
    </main>

    <%= render "shared/footer" %>
  </body>
</html>
