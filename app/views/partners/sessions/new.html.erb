<div class="min-h-screen bg-white">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 py-12">
    <%= link_to "← Back", user_flow_path, class: "text-slate-500 hover:text-slate-700 text-sm" %>

    <div class="mt-8 grid grid-cols-1 lg:grid-cols-12 gap-10 items-start">
      <div class="lg:col-span-7">
        <div class="rounded-3xl border border-slate-200 bg-white p-8 sm:p-10 shadow-sm">
          <p class="text-sm font-semibold text-slate-700">ComeInBooked for professionals</p>
          <h1 class="mt-2 text-3xl font-semibold tracking-tight text-slate-900">Sign in</h1>
          <p class="mt-2 text-slate-600">Manage services, bookings and payments.</p>

          <% if alert.present? %>
            <div class="mt-6 rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">
              <%= alert %>
            </div>
          <% end %>

          <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <div class="mt-8 space-y-5">
              <div>
                <%= f.label :email, class: "block text-sm font-medium text-slate-700 mb-1" %>
                <%= f.email_field :email, autofocus: true, autocomplete: "email",
                      class: "w-full rounded-xl border border-slate-200 bg-white px-4 py-3 focus:outline-none focus:ring-2",
                      style: "--tw-ring-color:#2f66ff;" %>
              </div>

              <div>
                <%= f.label :password, class: "block text-sm font-medium text-slate-700 mb-1" %>
                <%= f.password_field :password, autocomplete: "current-password",
                      class: "w-full rounded-xl border border-slate-200 bg-white px-4 py-3 focus:outline-none focus:ring-2",
                      style: "--tw-ring-color:#2f66ff;" %>
              </div>

              <button type="submit"
                      class="w-full rounded-xl px-5 py-3 font-semibold text-white shadow-sm"
                      style="background:#2f66ff;">
                Sign in
              </button>

              <div class="text-sm text-slate-600">
                New business?
                <%= link_to "Create a professional account", new_registration_path(resource_name),
                    class: "text-blue-700 hover:underline font-medium" %>
              </div>
            </div>
          <% end %>
        </div>
      </div>

      <div class="lg:col-span-5">
        <div class="rounded-3xl p-8 border border-slate-200 overflow-hidden relative"
             style="background:
               radial-gradient(900px 500px at 20% 10%, rgba(47,102,255,0.20) 0%, rgba(11,31,70,0) 60%),
               linear-gradient(180deg, #0b1f46 0%, #081733 100%);">
          <h2 class="text-2xl font-semibold text-white">Professional portal</h2>
          <p class="mt-3 text-white/70 text-sm">
            You’ll be able to set services, staff, availability, deposits and get reports.
          </p>

          <div class="mt-7 space-y-3 text-sm text-white/85">
            <div class="rounded-2xl border border-white/15 bg-white/5 p-4">📅 Calendar & rules (next)</div>
            <div class="rounded-2xl border border-white/15 bg-white/5 p-4">💳 Deposits (Stripe)</div>
            <div class="rounded-2xl border border-white/15 bg-white/5 p-4">📈 Growth tools (later)</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
